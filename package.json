{
  "name": "sm2-fsrs-bridge",
  "version": "0.1.0",
  "description": "Convert spaced repetition scheduling data between Anki's SM-2 fields and FSRS memory states",
  "type": "module",
  "bin": {
    "sm2-fsrs-bridge": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
